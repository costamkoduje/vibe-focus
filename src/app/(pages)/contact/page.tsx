import React from "react";
import { ContactHeroSection } from "@/components";

export default function ContactPage(): React.JSX.Element {
  return (
    <main className="flex flex-col w-full">
      <ContactHeroSection />
    </main>
  );
}
